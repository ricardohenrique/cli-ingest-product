APP_ENV=prod
APP_SECRET=changeme

# Path to the JSONL feed file inside the container (can also be passed as CLI argument)
FEED_INPUT_PATH=/data/coffee_feed.jsonl

# PostgreSQL connection — matches the docker-compose postgres service defaults
DATABASE_URL=postgresql://app:app@postgres:5432/app?serverVersion=16&charset=utf8
